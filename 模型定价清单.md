# 模型节点积分定价清单

> 适用范围：当前仓库里可见的 Flow/模型节点。
> 口径说明：**以后端实际扣费逻辑为准**，主要依据：
> `backend/src/credits/credits.service.ts`、
> `backend/src/credits/credits.config.ts`、
> `backend/src/admin/services/node-config.service.ts`、
> `backend/src/ai/services/model-routing.service.ts`。
>
> 文中把“节点默认值 / UI 兜底”和“后端实扣规则”分开写，避免把旧前端 fallback 当成真实价格。

## 1. 定价总原则

- `100 积分 = 1 元`。
- 优先级上，**后端动态定价 > 统一模型管理 pricing > 节点默认 `creditsPerCall`**。
- 同一个节点如果支持多模型、多通道、多分辨率、多时长，最终实扣通常不是一个固定值。
- 免费节点也列出来，确保“一个不漏”。

## 2. 图像类节点

| 节点 | 实际模型/服务 | 实扣积分 | 说明 |
| --- | --- | --- | --- |
| `generate` 生成节点 | `gemini-2.5-image` | 普通/极速：`1K=20`；尊享：`1K=40` | Fast 文生图，单张 |
| `generate4` 四图生成 | `gemini-2.5-image` | `generate` 单张价格 `x4` | 默认 4 张 |
| `generatePro` 自定义节点 | `gemini-3-pro-image` | 普通：`1K=40` `2K=60` `4K=80`；尊享：`1K=90` `2K=100` `4K=170`；极速：`1K=100` `2K=100` `4K=179` | Pro 文生图，单张 |
| `generatePro4` 高级四图 | `gemini-3-pro-image` | `generatePro` 单张价格 `x4` | 默认 4 张 |
| `generateReference` 参考生成 | `gemini-image-blend` | 普通：`1K=40` `2K=60` `4K=80`；尊享：`1K=90` `2K=100` `4K=170`；极速：`1K=100` `2K=100` `4K=179` | 多参考图融合/参考生成 |
| `gptImage2` | `gpt-image-2` | 普通：`1K=20` `2K=30` `4K=40`；尊享按质量：`low=30/35/40`、`medium=65/110/160`、`high=190/350/560`（对应 `1K/2K/4K`） | 支持文生图/图生图 |
| `analysis` / Image Chat | `gemini-2.5-image-analyze` 及同类 | `10` | Fast/Pro/Ultra 已统一到 10 |
| `seedream5` | Seedream 4.0 / 4.5 / 5.0 | 当前代码里明确可确认的是 `5.0: 1K=30, 2K=30, 4K=60` | 前端支持 `4.0/4.5/5.0` 和 `3K`，但仓库里**未看到独立完整扣费矩阵**；现有预览仍走同一服务桶，建议把这一点当“待产品确认” |
| `midjourneyV7` | `midjourney-v7` / `midjourney-v8` | `50` | Midjourney 节点内切 `v7/v8`，当前 imagine 统一按 50 |
| `niji7` | `midjourney-niji-7` | `50` | Niji 7 |
| `viewAngle` | 前端节点本地定价 | `30` | 视角变换节点；当前仓库未看到更细后端动态矩阵 |
| `imageGrid` | 本地拼接 | `0` | 免费 |
| `imageSplit` | 本地拆分 | `0` | 免费 |
| `imageCompress` | 本地压缩 | `0` | 免费 |
| `image` / `imagePro` | 输入节点 | `0` | 免费 |

### Banana 图像通道补充

对应 `generate / generate4 / generatePro / generatePro4 / generateReference`：

| 档位 | 普通 | 尊享 | 极速 |
| --- | --- | --- | --- |
| Fast | `1K=20` | `1K=40` | `1K=20` |
| Pro | `1K=40` `2K=60` `4K=80` | `1K=90` `2K=100` `4K=170` | `1K=100` `2K=100` `4K=179` |
| Ultra | `0.5K=30` `1K=30` `2K=40` `4K=50` | `0.5K=30` `1K=40` `2K=50` `4K=110` | `0.5K=50` `1K=50` `2K=75` `4K=113` |

说明：

- Fast 对应 `gemini-2.5-image`。
- Pro 对应 `gemini-3-pro-image` / `gemini-image-blend`。
- Ultra 对应 `gemini-3.1-image` 及其 edit/blend。
- 四图节点按单张价格乘以输出张数扣费。

## 3. 视频类节点

| 节点 | 实际模型/服务 | 实扣积分 | 说明 |
| --- | --- | --- | --- |
| `kling26Video` | `kling-2.6-video` | 见下表 | 按是否有声、`std/pro`、`5s/10s` 动态计费 |
| `kling30Video` | `kling-3.0-video` | 见下表 | 按是否有声、`std/pro`、`5s/10s` 动态计费 |
| `klingO1Video` / `klingO3Video` | `kling-o3-video` | `1600`（节点默认）/ `600`（Omni 固定服务桶） | 仓库里同时存在两套口径：旧 `klingO1Video` 默认 1600，新 `klingO3Video` 与后端 `kling-o3-video` 按 600 固定桶处理；以当前实际运行节点为准时，应优先看节点类型和请求参数 |
| `viduVideo` | `vidu-q2` / `vidu-q3` | 默认/兜底 `600`；`vidu-q3` 在腾讯 VOD Q3 Turbo 模板下可按分辨率线性计费 | Q3 Turbo：`540P=25/s`、`720P=37.5/s`、`1080P=50/s`，例如 5 秒分别是 `125/188/250` |
| `doubaoVideo` | `seedance-1.5-pro` | `600` | Seedance 1.5 Pro |
| `seedance20Video` | `seedance-2.0` / `seedance-2.0-fast` | 见下表 | 当前仓库默认折扣率实际为 `1`，不是旧文档里的 35 折 |
| `seedVideo` | `seed-2.0-pro/lite/mini` | `pro=600`、`lite=150`、`mini=100` | 这是后端按模型真实扣费；不是节点默认 600 一刀切 |
| `sora2Video` | `sora-sd` | `sora-2=200`、`sora-2-vip=200`、`sora-2-pro=750` | 节点默认 900 只是旧兜底，后端实扣按模型档位 |
| `sora2Character` | 角色提取 | `0` | 当前不单独计费 |
| `wan26` | `wan26-video` | `600` | 当前仓库未见更细动态矩阵 |
| `wan2R2V` | `wan26-r2v` | `600` | 当前仓库未见更细动态矩阵 |
| `wan27Video` | `wan27-video` | `600` | 当前仓库未见更细动态矩阵 |
| `happyhorseR2V` | `happyhorse-r2v-video` | `720P=120/s`、`1080P=200/s` | 例如 5 秒分别是 `600/1000` |
| `videoAnalyze` | `gemini-video-analyze` | 见下表 | 按文本档位 + 通道动态计费 |
| `videoToGif` | `video-to-gif` | `30` | 固定价 |
| `volcEnhanceVideo` | `volc-enhance-video` | 见下表 | 按标准/专业、分辨率、帧率动态计费 |
| `videoFrameExtract` | 本地提帧 | `0` | 免费 |
| `video` | 输入节点 | `0` | 免费 |

### Kling 2.6 动态计费

| 是否有声 | 模式 | 5 秒 | 10 秒 |
| --- | --- | --- | --- |
| 无声 | `std` | `150` | `300` |
| 无声 | `pro` | `300` | `500` |
| 有声 | `std` | `500` | `1000` |
| 有声 | `pro` | `600` | `1200` |

### Kling 3.0 动态计费

| 是否有声 | 模式 | 5 秒 | 10 秒 |
| --- | --- | --- | --- |
| 无声 | `std` | `300` | `600` |
| 无声 | `pro` | `400` | `800` |
| 有声 | `std` | `450` | `900` |
| 有声 | `pro` | `600` | `1200` |

### Seedance 2.0 动态计费

| 模型 | 分辨率 | 单价 | 5 秒示例 |
| --- | --- | --- | --- |
| `seedance-2.0-fast` | `480P` | `80.6/s` | `403` |
| `seedance-2.0-fast` | `720P` | `96.6/s` | `483` |
| `seedance-2.0` | `480P` | `100/s` | `500` |
| `seedance-2.0` | `720P` | `120/s` | `600` |
| `seedance-2.0` | `1080P` | `300/s` | `1500` |
| `seedance-2.0` | `4K` | `600/s` | `3000` |

补充：

- 若环境变量 `SEEDANCE20_FREE=1/true/on/yes`，则 `seedance-2.0 / seedance-2.0-fast` 全规格 `0` 积分。

### 视频分析节点动态计费

| 通道 | Fast | Pro | Ultra |
| --- | --- | --- | --- |
| 普通 | `60` | `90` | `120` |
| 尊享 | `80` | `120` | `160` |
| 极速 | `60` | `100` | `130` |

### 火山视频增强动态计费

| 版本 | 分辨率 | `<=30fps` | `>30fps` |
| --- | --- | --- | --- |
| `standard` | `720P` | `90` | `180` |
| `standard` | `1080P` | `180` | `360` |
| `standard` | `2K` | `360` | `720` |
| `standard` | `4K` | `720` | `1440` |
| `professional` | `720P` | `750` | `1500` |
| `professional` | `1080P` | `1500` | `3000` |
| `professional` | `2K` | `3000` | `6000` |
| `professional` | `4K` | `6000` | `12000` |

## 4. 文本 / 分析 / PPT 类节点

| 节点 | 实际模型/服务 | 实扣积分 | 说明 |
| --- | --- | --- | --- |
| `textChat` | `gemini-text` | 普通 `5`，尊享 `10`，极速 `Fast=5/Pro=10/Ultra=10` | Banana/Gemini 文本链路 |
| `textChat` 切到 DeepSeek | `deepseek-v4-flash / deepseek-v4-pro` | `30 / 60` | 前端单独写死了 DeepSeek 价格 |
| `promptOptimize` | `gemini-prompt-optimize` | 普通 `5`，尊享 `10`，极速 `Fast=5/Pro=10/Ultra=10` | 后端动态预览 |
| `promptOptimize` 切到 DeepSeek | `deepseek-v4-flash / deepseek-v4-pro` | `30 / 60` | 前端单独写死了 DeepSeek 价格 |
| `storyboardSplit` | `gemini-text` | `10` | 当前节点目录里写的是固定 10 |
| `htmlPpt` | `gemini-text` | 普通 `5`，尊享 `10` | 前端节点走同一文本计费预览 |
| `textPrompt` / `textPromptPro` / `textNote` | 输入/便签 | `0` | 免费 |

## 5. 3D / 音频类节点

| 节点 | 实际模型/服务 | 实扣积分 | 说明 |
| --- | --- | --- | --- |
| `three` | `convert-2d-to-3d` | `200` | 2D 转 3D |
| `seed3d` | `convert-2d-to-3d` | `300` | Prompt/图片生成 3D |
| `audioStudio` 豆包 Seed Audio | `doubao-seed-audio-1-0` | `2/s` | 线性计费，5 秒就是 `10` |
| `audioStudio` MiniMax Speech 2.6 HD | `minimax-speech-2.6-hd` | `10` | 固定价 |
| `audioStudio` MiniMax Speech 2.5 | `minimax-speech-2.5` | `10` | 固定价 |
| `audioStudio` MiniMax Music 2.5+ | `minimax-music-2.5+` | `30` | 固定价 |
| `audioStudio` MiniMax Music 2.5 | `minimax-music-2.5` | `30` | 固定价 |
| `audioStudio` 腾讯视频配音 | `tencent-dub` | `10` | 固定价 |
| `audioStudio` 导入模式 | 本地导入 | `0` | 免费 |

## 6. 当前仓库里的几个重要口径差异

### 6.1 节点默认值不等于真实扣费

- `sora2Video` 节点默认 `900`，但后端真实扣费按模型：`200/200/750`。
- `seedVideo` 节点默认 `600`，但后端真实扣费按模型：`600/150/100`。
- `kling26Video / kling30Video / happyhorseR2V / seedance20Video / volcEnhanceVideo` 都是动态计费节点，不能只看卡片上的基础值。

### 6.2 前端某些 fallback 还是旧值

- `frontend/src/services/nodeConfigService.ts` 里的默认配置有历史残留，例如：
  - `textChat/promptOptimize` 仍有 `10` 的 fallback；
  - 后端真实默认已是 `5`。
- 所以对外文档和运营说明应统一以后端价格为准。

### 6.3 Seedream 目前最容易误解

- 前端节点支持 `4.0 / 4.5 / 5.0`、`2K / 3K / 4K`。
- 但当前仓库里明确可落地核对到的静态 pricing 配置，只有 `doubao-seedream-5-0-260128`。
- 如果要给运营/用户写成最终对外价格表，**建议先确认 4.0、4.5、3K 是否有独立扣费规则**；否则最稳妥的写法是“当前系统可确认基准价：5.0 的 `2K=30`、`4K=60`”。

## 7. 最简版可直接给产品/运营的摘要

- 文生图 Fast：`20` 起。
- 文生图 Pro：`40` 起。
- 文生图 Ultra：`30` 起。
- GPT-Image-2：普通 `20/30/40`，尊享最高到 `560`。
- 图片分析：`10`。
- 文本对话/提示词优化：普通 `5`，尊享 `10`，DeepSeek `30/60`。
- Kling 2.6：`150` 到 `1200`。
- Kling 3.0：`300` 到 `1200`。
- Seedance 2.0：按分辨率和时长线性计费，5 秒 `403` 到 `3000`。
- Seed 2.0：`100 / 150 / 600`。
- Sora2：`200 / 750`。
- Wan 2.6 / 2.7：当前按 `600` 口径。
- HappyHorse：`120/s` 或 `200/s`。
- 视频增强：`90` 到 `12000`。
- 2D 转 3D：`200`；Seed 3D：`300`。
- 音频工作台：豆包 Seed Audio `2/s`，MiniMax 语音 `10`，MiniMax 音乐 `30`，腾讯配音 `10`。

